# Baz website verification — 2026-09-06

The GitHub Pages site belongs to Baz’s repository. This receipt covers its
static artifact and browser behavior, not the HTTP framework runtime.
No Zig source or dependency pin changed; no benchmark was run.

## Local checks

The site builder passed its link, anchor, allowlist, example-count, SVG, source
identity, and benchmark-summary checks. JavaScript/Python syntax and actionlint
passed. All current guide/README Markdown file targets exist locally.

Chrome `152.0.7977.76` on the native M3 Max Mac rendered the artifact under a
`/baz/` project prefix. The [browser receipt](2026-09-06-website/browser.json)
records 14 passing groups:

- Desktop landing page, four diagrams, and source-derived code highlighting.
- Exact identity of the maintained `Hello` excerpt.
- Keyboard skip-link focus.
- All six example-filter choices and their counts.
- Desktop diagrams and all four benchmark profiles.
- Mobile menu, Escape, section navigation, and no document overflow at widths
  320, 390, 760, 820, 1024, and 1440.
- Reader section fragments and generated contents.
- Highlighted source and raw/revision-pinned GitHub links.
- Mobile reader and contents.
- Six invalid/unpublished reader paths rejected before document fetch.
- Malicious Markdown sanitized before insertion; no injected script, form,
  frame, style, or external image capability became active.
- All 58 allowlisted documents rendered at mobile width without page overflow.
- Print output and the landing page without JavaScript.
- No JavaScript exceptions or unexpected external asset requests/errors.

The deliberate malicious-style input produced one expected CSP rejection during
sanitizer parsing. The receipt distinguishes that defense check from ordinary
page errors. The PDF was tagged, contained no JavaScript, and had 12 pages;
the first page and desktop/mobile/reader/chart screenshots were visually reviewed.
Temporary screenshots, browser profile, and PDF remain ignored local artifacts.

The initial browser pass found narrow-width fact-row overflow and section-fragment
scroll timing. Both were corrected before the passing run above. The
[authored source manifest](2026-09-06-website/source.sha256.json) identifies the
site and check scripts exercised by that run. The Pages builder generates fresh
revision links after the integration commit; pre-commit artifacts are not pushed.

## Host ownership

An atomic shared-host reservation covered each finite local browser run.
The final run used a 180-second supervisor timeout and isolated browser process
groups. The [cleanup receipt](2026-09-06-website/cleanup.json) records absent
owned groups; the preview server stopped before releasing the owned lock.
No HTTP server test, performance client, or Linux workload ran for this change.

## Publication

The Pages workflow builds the artifact from Baz’s `main`, checks it, and deploys
through GitHub Actions. The live artifact’s `publication.json` identifies its
commit and file hashes. Deployment and hosted native CI results are available in
[Baz Actions](https://github.com/technologylab-ai/baz/actions).

The Windows dependency update remains on the separate paused draft branch.

## GitHub Pages path check

The initial deployment passed, and 77 public payload files matched their hashes.
The live host hides dot-prefixed paths: `.nojekyll` is an artifact control file,
while `.zig-version` needed a public reader alias. The builder now serves that
unchanged document at `docs/zig-version.txt`, keeping its canonical name for
reader navigation and GitHub links.

The [follow-up browser receipt](2026-09-06-website/pages-path-fix-browser.json)
again passed all 14 groups and 58 documents on the updated
[source manifest](2026-09-06-website/pages-path-fix-source.sha256.json).
The [cleanup receipt](2026-09-06-website/pages-path-fix-cleanup.json) records
released owned browser groups and reservation.

## Live publication verified

The live Pages deployment at `fab1c36b46ab5f51a14d5f04a9a016ca5840e718`
passed the same [14 browser groups](2026-09-06-website/public-browser.json),
including all 58 published documents, mobile layout, keyboard behavior, and
sanitization. All [78 public payload files](2026-09-06-website/public-files.json)
matched the deployment manifest’s hashes; `.nojekyll` was excluded as a hidden
build-control file. The [public-browser cleanup](2026-09-06-website/public-cleanup.json)
records absent owned process groups.

[Deployment run 34046029915](https://github.com/technologylab-ai/baz/actions/runs/34046029915)
succeeded. The website is integrated into Baz’s `main`, with its own public
repository and HTTPS Pages site.

## Pure Zig, prominent engine link, and Windows support

After Baz’s native Windows gate passed, the site added Windows x64 to its platform
summary and engine diagram, a link to Baz’s own native receipt, a pure-Zig
introduction, and direct bounded/http website links in the top bar and hero.
The updated local artifact passed [all 14 browser groups with 59 documents](2026-09-06-website/windows-site-browser.json).
Desktop/mobile/engine screenshots were reviewed. The
[cleanup receipt](2026-09-06-website/windows-site-cleanup.json) records released
browser groups and the shared-host reservation. The final desktop rail wraps
Windows x64 onto its own line for readability.
